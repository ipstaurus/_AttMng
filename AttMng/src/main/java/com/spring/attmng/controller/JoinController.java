package com.spring.attmng.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

import com.spring.attmng.dto.EmployeeDTO;
import com.spring.attmng.service.JoinService;

@Controller
public class JoinController {

	/*
	 * @Autowired private JoinService joinService;
	 * 
	 * //Join GET
	 * 
	 * @RequestMapping(value = "/join", method = RequestMethod.GET) public void
	 * joinGET() throws Exception{
	 * 
	 * }
	 * 
	 * //Join PSOT
	 * 
	 * @RequestMapping(value = "/join", method = RequestMethod.POST) public String
	 * joinPOST(EmployeeDTO employeeDto) throws Exception{
	 * 
	 * joinService.insertMember(employeeDto);
	 * 
	 * return "/join"; }
	 */
}

